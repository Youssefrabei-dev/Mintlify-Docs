---
title: "Mobile docs"
description: "Reserved home for future SawaMobile documentation after backend docs stabilize."
keywords: ["mobile", "SawaMobile", "future docs"]
---

Detailed mobile docs are deferred until the backend documentation and OpenAPI contract workflow are stable.

For now, backend developers should use the API contract concept page to understand the producer side of `/contract/openapi`, `X-Sawa-Contract-Key`, and `API_CONTRACT_VERSION`.

<Note>
  Future mobile docs should cover SawaMobile setup, API type generation, auth token handling, and contract verification from the mobile consumer side.
</Note>
